//! Universal RTOS Native Simulator — host executable.
//!
//! Loads a configuration, compiles the guest firmware, spawns the simulator
//! core, and runs the event loop.

fn main() {
    env_logger::init();
    log::info!("sim-runner starting (stub)");
}
